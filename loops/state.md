# Cognistration State

Last updated: 2026-06-22

Platform facts
- Product name: Cognistration
- Domain: bishoptech.dev
- Website/runtime repo: /Users/matthewbishop/BishopTech.dev/bishoptech-cognistration
- iOS app: separate app; no shared account or sign-in with the website
- Monetization split:
  - iOS: Apple paywall
  - Website: Stripe
- Stripe webhook endpoint: https://bishoptech.dev/api/webhooks/stripe

Operating goals
- Keep the website, tone library, and iOS surface in sync.
- Keep Stripe checkout, webhook delivery, and billing state healthy.
- Expand the tone library over time with at least 2 new tone variants per loop session when tone work is touched.
- Keep the backend healthy and buildable after tone or blog additions.
- Audit one backend section per loop so coverage rotates over time.
- Track BishopTech.dev SEO growth as a separate content-growth loop inside the same repo.

Monitoring checklist
- Site build passes
- Runtime health route responds
- Stripe webhook endpoint responds and verifies signatures
- Checkout sessions can be created with current env vars
- Payments, refunds, invoices, and subscription events are flowing
- Tone generation path still works
- Blog sharing path still works
- iOS sync assumptions still hold
- Stripe/Apple paywall assumptions still hold
- BishopTech.dev growth loop has a current 3-day trend watchlist

Open questions
- Where the iOS app repo will move next
- Which exact backend sections should get the highest priority
- Which blog-to-iOS sync path is currently authoritative
- Whether tone generation should be fully automated or gated by a manual review step
- Which BishopTech.dev query clusters are strongest enough to turn into published service pages

Current loop focus
- Morning: tone variants + content sync + quick build check
- Evening: backend section audit + bug sweep + build verification
- Rotation: auth/paywall, tone generation, blog publish/share, app sync, analytics, Stripe health/webhooks, admin/ops, BishopTech.dev growth

Loop note
- 2026-06-14 morning: detected new homepage tone variants (`theta-liminal-settle` and `alpha-clear-window`) in the tone library, confirmed `npm run build` passes, verified the site runtime on :3001 returns healthy on `GET /api/health`, and checked that `/blog` plus `sitemap.xml` still cover the blog/contact/AI disclosure surfaces. App-sync assumptions still hold: the iOS surface is documented as a separate app with no shared website sign-in, and the authoritative iOS sync contract is still unresolved in docs.
- 2026-06-13 morning: added theta-liminal-settle and alpha-clear-window tone variants, regenerated homepage wavs, confirmed `npm run build`, and verified `GET /api/health` returns healthy on the running runtime.
- 2026-06-14 evening: audited tone generation. `npm run build` still passes with the existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; `/api/health` is healthy on the runtime; `/api/audio/preview-tone` returns `homepage-theta-drift`; and the homepage tone library still resolves the newer `homepage-theta-liminal-settle` and `homepage-alpha-clear-window` entries through the shared helpers. Blog publish/share still has the static blog route and share/write path in place. App-sync remains documented as a separate app/client path with no shared website sign-in, but the authoritative iOS sync contract is still unresolved in docs.
- 2026-06-14 site SEO cleanup: added route-specific head tags for `/blog`, `/pricing`, and `/tutorial`, and added canonical URLs to the main legal/contact pages so public snippets stop inheriting the root default.
- 2026-06-15 morning: tone work remains expanded with `homepage-theta-liminal-settle` and `homepage-alpha-clear-window`; `npm run build` passed cleanly with only the pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; `/api/health` is healthy on :3001; `/blog`, `/contact`, `/cookies`, `/ai-disclosure`, `robots.txt`, and `sitemap.xml` still cover the public content surfaces; `community` still returns 500 without Supabase env vars; and app-sync assumptions still hold because the iOS surface is documented as a separate app with no shared website sign-in while the authoritative sync contract is still unresolved in docs.
- 2026-06-15 evening: audited app sync and runtime surfaces. `npm run build` passes cleanly; `/api/health` on :3001 is healthy; `/blog` returns 200 with the archive and canonical blog URLs; `/sitemap.xml` returns 200 and includes the blog/static public routes; app-sync assumptions still hold because the iOS surface is documented as a separate app with no shared website sign-in, while `/community` still returns 500 without Supabase env vars, so the local community/iOS-sync path remains blocked.
- 2026-06-16 morning: tone work expanded again with `homepage-theta-quiet-window` and `homepage-alpha-open-channel`; `npm run build` passed cleanly with the same pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; `/api/health` on :3001 returned healthy; `/blog` and `/sitemap.xml` both responded 200; and app-sync assumptions still hold because the iOS surface remains documented as a separate app with no shared website sign-in, while the authoritative iOS sync contract is still unresolved in docs.
- 2026-06-16 evening: audited analytics. `npm run build` still passes with the same pre-existing hook dependency warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; `/api/health` on :3001 is healthy; `/blog` returns 200; `/community` still returns 500 without Supabase env vars; and `POST /api/audio/combined` returned analytics successfully with `ok: true`, `lengthSec: 180`, `baseFreqHz: 220`, coverage split across theta/alpha, and a 5-stage summary. Blog publish/share still looks healthy from the live archive route, and app-sync assumptions still hold because the iOS surface remains documented as a separate app with no shared website sign-in.
- 2026-06-17 morning: changed tone work still resolves four homepage state variants, with the two newest additions being `homepage-theta-quiet-window` and `homepage-alpha-open-channel`; `npm run build` passed cleanly with the same pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; the runtime is healthy on :3001 with `GET /api/health` returning `status: healthy`; `/blog`, `/sitemap.xml`, `/llms.txt`, and the public legal/contact surfaces still resolve as expected; app-sync assumptions still hold because the iOS surface remains a separate app with no shared website sign-in, while the authoritative iOS sync contract is still unresolved in docs; `/community` still returns 500 without Supabase env vars, so the local community/iOS-sync path remains blocked.
- 2026-06-17 evening: audited admin/ops. `npm run build` still passes with the same pre-existing hook dependency warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; `GET /api/health` on :3001 returned `status: healthy` with `agentic_tones_count: 105`; `/blog` and `/sitemap.xml` both returned 200; the internal admin/library generation route remains present and wired to the Supabase admin client; blog publish/share and app-sync assumptions still hold because the public blog surface is live and the iOS surface is still documented as a separate app with no shared website sign-in.
- 2026-06-18 build verification: installed repo dependencies with `npm install`, then confirmed `npm run build` completes successfully on Next 15.5.14. Remaining build output is limited to the pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`, plus a stale Browserslist database notice. Live route smoke checks on port 3011 returned 200 for `/`, `/llms.txt`, `/robots.txt`, and `/sitemap.xml`.
- 2026-06-18 morning: tone work expanded again with `homepage-theta-cognistration-arc` and `homepage-alpha-cognistration-aperture`, bringing the homepage state set to six variants; `npm run build` still passes cleanly with the same pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; the live runtime on :3011 returned healthy on `GET /api/health` with `agentic_tones_count: 105`; `/blog`, `/sitemap.xml`, `/llms.txt`, and `/robots.txt` still resolve on the running site; and app-sync assumptions still hold because the iOS plan still describes a separate app with low-friction sign-in and no shared website account while the authoritative sync contract remains unresolved in docs.
- 2026-06-18 afternoon: tightened the public BishopTech growth surface by adding a homepage trust note, making `public/llms.txt` more explicit about the product and policy pages, and adding `ContactPoint`/`WebPage` JSON-LD to the root layout. Verified with `npm run build`, then smoke-checked the live site on port 3011: `/` returned 200, `/llms.txt` reflected the updated public reference, and `/robots.txt` plus `/sitemap.xml` returned 200.
- 2026-06-18 evening: audited auth/paywall. `npm run build` still passes after fixing the Stripe webhook handler to treat checkout sessions and subscription events separately; the live runtime on :3011 is healthy on `GET /api/health` with `agentic_tones_count: 105`; `/pricing`, `/blog`, `/llms.txt`, `/robots.txt`, and `/sitemap.xml` all return 200; `/api/checkout` and `/api/webhooks/stripe` return 503 locally because Stripe secrets are not present in the runtime env, so live payment execution remains blocked until those env vars are provided. Blog publish/share still holds via the live archive route, and app-sync assumptions still hold because the iOS surface remains a separate app with no shared website sign-in.
|- 2026-06-19 morning: tone work expanded again with `homepage-theta-cognistration-arc` and `homepage-alpha-cognistration-aperture`, bringing the homepage tone library to 11 entries; `npm run build` completed successfully on Next 15.5.14 with only the pre-existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; the live runtime on :3011 returned healthy on `GET /api/health` with `agentic_tones_count: 105`, and `/blog`, `/ai-disclosure`, `/llms.txt`, and `/sitemap.xml` all returned 200. App-sync assumptions still hold: the iOS surface remains documented as a separate app with no shared website sign-in, while the sync contract remains unresolved in docs.
|- 2026-06-19 evening: audited tone generation. `npm run build` passed on Next 15.5.14; the live runtime on :3011 stayed healthy on `GET /api/health`; `GET /api/audio/preview-tone` still resolves `homepage-theta-drift`; `/blog` and `/blog/pulvinar-precision-broker` both return 200, with article JSON-LD present on the post page; and `sitemap.xml` still returns 200. Blog publish/share remains healthy via the archive and article routes. App-sync assumptions still hold operationally at the loop-state level, but the iOS roadmap doc still conflicts by saying web/mobile should share the same account and session model, so the authoritative sync contract remains unresolved.
|- 2026-06-20 morning: tone work is still expanding; `lib/audio/homepage-tones.js` now includes the new `homepage-delta-cognistration-anchor` and `homepage-gamma-cognistration-beacon` variants alongside the earlier theta/alpha additions, `npm run build` completed successfully with only the existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`, the runtime on :3011 stayed healthy on `GET /api/health`, `/blog` and `/blog/pulvinar-precision-broker` both returned 200 with article JSON-LD present, and `sitemap.xml` returned 200. Blog publish/share still looks healthy, and app-sync assumptions still hold at the operating level because the iOS surface is documented as a separate app with no shared website sign-in, but the roadmap doc continues to conflict by stating web/mobile share the same account and session model.
- 2026-06-20 compliance/SEO audit: verified privacy, terms, cookies, contact, AI disclosure, and health warning pages; confirmed homepage/footer/legal navigation links; confirmed `llms.txt`, `robots.txt`, and `sitemap.xml` all return 200 on the live build; verified canonical, OG, Twitter, and JSON-LD on `/`, `/blog`, `/privacy`, `/terms`, `/cookies`, `/contact`, `/ai-disclosure`, `/machine`, `/tutorial`, and a representative blog post; no content edits were needed.
- 2026-06-20 evening: audited blog publish/share. `npm run build` passed; the live runtime on `:3011` is healthy on `GET /api/health` with `agentic_tones_count: 105`; `/blog` returns 200 with the archive and recent-posts surface; `/blog/pulvinar-precision-broker` returns 200 with article JSON-LD present; and `sitemap.xml` still includes the blog and static public routes. Blog publish/share is healthy, so the rotation moves to app sync.
- 2026-06-22 evening: audited analytics. `npm run build` passed on Next 15.5.14 with the existing hook warnings in `components/dashboard/SettingsView.jsx` and `components/dashboard/WorkshopComposer.jsx`; the live runtime on `:3011` is healthy on `GET /api/health` with `agentic_tones_count: 105`; `/blog`, `/blog/pulvinar-precision-broker`, `/community`, and `sitemap.xml` all returned 200; and `POST /api/audio/combined` returned analytics successfully with `ok: true`, `lengthSec: 180`, `baseFreqHz: 220`, theta/alpha coverage, and stage summaries. Blog publish/share still looks healthy, and app-sync assumptions still hold because the website/iOS split remains documented as separate. Small bug found: `/api/audio/combined` still labels its output as `mp3` while the current encoder/persist path produces `webm`, so `mp3` stays null; smallest next fix is to align the combined route artifact naming with the actual encoder output or add mp3 support end-to-end.
- Next section: admin/ops.
- Blocker: reconcile the iOS sync contract docs before treating app-sync as authoritative; combined route output naming is also mismatched until the encoder/persistence path is aligned.
