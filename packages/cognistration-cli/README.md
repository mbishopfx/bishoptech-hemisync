# @cognistration/cli

Source-distributed CLI for checking Cognistration's public contract.

```bash
npx @cognistration/cli capabilities
npx @cognistration/cli tools
npx @cognistration/cli sandbox
```

The CLI is read-only. It does not create accounts, start audio, submit checkout, or accept payment credentials.
